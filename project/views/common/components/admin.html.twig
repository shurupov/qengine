{% macro pageProperties(page, slug, editMode) %}

    {% import "/common/components/data.html.twig" as data %}

    {% if editMode %}
        <input type="checkbox" name="toggle" id="toggle" />
        <div class="message">
            <label id="settings-label" class="btn" for="toggle">Параметры страницы</label>
            <h1>Параметры страницы</h1>
            <h2>Title {{ data.text(page.title, slug, 'title', editMode) }}</h2>
        </div>
    {% endif %}

{% endmacro %}

{% macro loginForm() %}

    <div class="container">

        <div class="row">

            <div class="col-sm-offset-3 col-md-offset-4 col-sm-6 col-md-4">

                <div class="title"><span>Вход в админку</span></div>

                <form action="" method="post">

                    <div class="form-group">
                        <label for="username">Имя пользователя</label>
                        <input type="text" id="username" name="username" class="form-control" autofocus/>
                    </div>

                    <div class="form-group">
                        <label for="password">Пароль</label>
                        <input type="password" id="password" name="password" class="form-control"/>
                    </div>

                    <button type="submit" class="btn btn-theme">
                        <i class="fa fa-long-arrow-right"></i>
                        Войти
                    </button>

                </form>

            </div>

        </div>

    </div>

{% endmacro %}

{% macro adminPanel(pages) %}

    {% set editMode = true %}

    {% import "/common/components/data.html.twig" as data %}

    <div class="container" style="margin-top: 180px;">
        <div class="row">
            <div class="col-sm-12">

                <table class="table table-hover">
                    <caption>Страницы сайта</caption>
                    <thead class="hidden-sm hidden-xs">
                        <tr>
                            <th>
                                <div class="row">
                                    <div class="col-md-6">URI</div>
                                    <div class="col-md-6">Заголовок</div>
                                </div>
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for page in pages %}
                        <tr>
                            <td>
                                <div class="row">
                                    <div class="col-md-6">{{ data.text(page.slug, page.slug, 'slug', editMode) }} <a href="/{{ page.slug }}">ссылка</a></div>
                                    <div class="col-md-6">{{ data.text(page.title, page.slug, 'title', editMode) }}</div>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>

            </div>
        </div>
    </div>

{% endmacro %}

{% macro admin(editMode, pages = []) %}

    {% import _self as admin %}

    {% if editMode %}
        {{ admin.adminPanel(pages) }}
    {% else %}
        {{ admin.loginForm() }}
    {% endif %}

{% endmacro %}