{% macro text(setting, editMode = false, placement = 'top') %}
    {% import '/common/components/data.html.twig' as data %}
    {{ data.text('setting', setting.value, setting._id, 'value', editMode, false, placement) }}
{% endmacro %}

{% macro textarea(setting, editMode = false, placement = 'top') %}
    {% import '/common/components/data.html.twig' as data %}
    {{ data.textarea('setting', setting.value, setting._id, 'value', editMode, false, placement) }}
{% endmacro %}

{% macro image(setting, editMode = false, default = '') %}
    {% import '/common/components/data.html.twig' as data %}
    {{ data.image('setting', setting.value, setting._id, 'value', editMode, setting.attrs, default, setting.settings) }}
{% endmacro %}

{% macro code(setting, editMode = false, placement = 'top') %}
    {% import '/common/components/data.html.twig' as data %}
    {{ data.code('setting', setting.value, setting._id, 'value', editMode, false, placement) }}
{% endmacro %}