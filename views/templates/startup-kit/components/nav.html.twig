{% macro pageNavSection(sectionData, pageId, path, editMode) %}

    {% import "/common/components/data.html.twig" as data %}

    <div class="page-nav-space-holder hidden-xs">
        <div id="page-nav-wrapper" class="page-nav-wrapper text-center">
            <div class="container">
                <ul id="page-nav" class="nav page-nav list-inline">
                    {% for i,link in sectionData.links %}
                        <li>{{ data.a(link, pageId, path ~ '.links.' ~ i, editMode, {'class': 'scrollto'}) }}{{ data.removeElementButton(path ~ '.links.' ~ i, pageId, editMode) }}</li>
                    {% endfor %}
                    {{ data.addElementButton(path ~ '.links', pageId, editMode, "Добавить ссылку") }}
                    {{ data.removeElementBigButton(path, pageId, editMode, "Удалить компонент") }}
                </ul><!--//page-nav-->

            </div>
        </div><!--//page-nav-wrapper-->
    </div><!--//page-nav-space-holder-->
{% endmacro %}